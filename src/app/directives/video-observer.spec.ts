import { VideoObserver } from './video-observer';

describe('VideoObserver', () => {
  it('should create an instance', () => {
    const directive = new VideoObserver();
    expect(directive).toBeTruthy();
  });
});
